{
  "name": "@laxdb/scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest",
    "typecheck": "tsgo --noEmit",
    "lint": "oxlint --type-aware --fix",
    "format": "oxfmt",
    "fix": "bun run lint && bun run format"
  },
  "dependencies": {
    "@laxdb/core": "*"
  },
  "devDependencies": {
    "@vitest/ui": "^3.2.4"
  }
}
