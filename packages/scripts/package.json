{
  "name": "@laxdb/scripts",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "typecheck": "tsgo --noEmit",
    "lint": "oxlint --fix",
    "format": "oxfmt",
    "fix": "bun run lint && bun run format"
  },
  "dependencies": {
    "@laxdb/core": "*"
  },
  "devDependencies": {
    "@vitest/ui": "^3.2.4"
  }
}
