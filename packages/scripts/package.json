{
  "name": "@laxdb/scripts",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "typecheck": "tsgo --noEmit",
    "lint": "oxlint",
    "format": "oxfmt",
    "fix": "oxlint --fix && oxfmt"
  },
  "dependencies": {
    "@laxdb/core": "*"
  },
  "devDependencies": {
    "@vitest/ui": "^3.2.4"
  }
}
