{
  "name": "@laxdb/scripts",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest",
    "typecheck": "tsc --noEmit",
    "format": "oxfmt ",
    "format:check": "bun run format -- --check",
    "fix": "oxlint --fix && oxfmt"
  },
  "dependencies": {
    "@laxdb/core": "*"
  },
  "devDependencies": {
    "@vitest/ui": "^3.2.4"
  }
}
